# -*-yaml-*- vim:syntax=yaml
{{ $emperor_ruppy := bitwardenFields "item" "f8d0f3e2-7d8b-4e23-befc-ae15017ae995" -}}
provider: Cloudflare
login_token: "{{ (bitwarden "item" "d7aa3e8d-1eaa-49ab-ba57-ae19000f11a9").notes | trim }}"
domains:
  - domain_name: latipun.dev
    sub_domains: [kumori, raspi]
ip_url: https://api.ip.sb/ip
ipv6_url: https://api-ipv6.ip.sb/ip
ip_type: IPv4
interval: 60
resolver: 1.1.1.1
user_agent:
ip_interface: eth0
socks5_proxy:
use_proxy: false
debug_info: false
notify:
  discord:
    enabled: false
    bot_api_token: "{{ $emperor_ruppy.botToken.value | trim }}"
    channel: "{{ $emperor_ruppy.godnsNotifyChannel.value | trim }}"
    message_template: "Domain **{{`{{ .Domain }}`}}** is updated to **{{`{{ .CurrentIP }}`}}**"
